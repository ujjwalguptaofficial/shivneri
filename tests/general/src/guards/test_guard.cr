module General
  class TestGuard < Guard
    def check
      # if (query["text"] == "as")
        # return text_result("blocked by guard")
      # else
      #   return nil_result
      # end
      # nil_result
    end
  end
end
